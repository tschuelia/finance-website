[project]
name = "finances"
version = "0.1.0"
requires-python = ">=3.11,<3.12"

[tool.ruff.lint]
select = [
  "F",    # flake8
  "I",    # isort
  "UP",   # pyupgrade
]

[tool.ruff.lint.isort]
known-first-party = [
    "accounting",
    "finances",
]
